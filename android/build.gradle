// Top-level build file
buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

allprojects {
    // 仓库已在 settings.gradle 中配置
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
